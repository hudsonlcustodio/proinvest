# ADR-017: Append-only valuation history

Status: Accepted.

Observed snapshots are appended with currency, known value, status, coverage, observation time and provenance. Past rows are never rewritten. A first point is not a trend; charts require at least two real points per series.
